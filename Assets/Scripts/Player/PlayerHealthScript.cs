using UnityEngine;

public class PlayerHealthScript : MonoBehaviour
{

    public int myLives = 4;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
